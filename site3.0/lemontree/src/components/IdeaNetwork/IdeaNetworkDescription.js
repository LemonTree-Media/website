import React from 'react';

function IdeaNetworkDescription() {
    return (
        <div className="mx-5 my-4">
          <h1>Idea Network</h1>
          <p>Each video on LemonTree is logged as an "idea," indexed carefully, and can be leveraged when creatives give you proposals. Creatives' jobs are solving your challenge, so we call them "solvers."</p>
        </div>
    );
}

export default IdeaNetworkDescription;

